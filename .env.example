# Frontend (Vite) — optional
VITE_API_URL=

# Contact form — SMTP / Nodemailer (server only — do NOT prefix with VITE_)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=your-email@gmail.com
CONTACT_RECEIVER_EMAIL=your-email@gmail.com

# Server
PORT=5000
CLIENT_URL=http://localhost:4028
